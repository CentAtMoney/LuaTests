{
    "tasks": [
        {
            "label": "make build",
            "command": "/usr/bin/sh",
            "args": [
                "-c",
                "cmake -DCMAKE_BUILD_TYPE=Debug ..; make"
            ],
            "options": {
                "cwd": "${workspaceFolder}/build"
            },
        }
    ],
    "version": "2.0.0"
}
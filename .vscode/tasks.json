{
    "tasks": [
        {
            "label": "make build",
            "command": "/usr/bin/sh",
            "args": [
                "-c",
                "cmake ..; make"
            ],
            "options": {
                "cwd": "${workspaceFolder}/build"
            },
        }
    ],
    "version": "2.0.0"
}